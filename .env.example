# Public canonical URL used by sitemap, robots, canonical links, and social metadata.
NEXT_PUBLIC_SITE_URL="https://sweed.com"

# Server-only AI advisor config. Do not prefix these with NEXT_PUBLIC_.
OPENAI_BASE_URL="https://rork-openai-proxy-v2.amremaad11.workers.dev/v1"
OPENAI_API_KEY="replace-with-server-only-key"
AI_MODEL="gpt-4o-mini"

# Local debug dashboard is disabled in production unless this is explicitly enabled.
AI_DEBUG_DASHBOARD_ENABLED="false"
