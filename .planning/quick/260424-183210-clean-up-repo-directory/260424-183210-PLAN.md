---
status: in_progress
type: quick-task
created: 2026-04-24
---

# Quick Task 260424-183210: Clean Up Repo Directory

## Goal

Clean the repository root while preserving the live static SWEED website, source design files, planning/Codex workflow files, and the `v2/` Git worktree.

## Tasks

1. Archive obvious root clutter and old generated/demo files into `.archive/root-cleanup-20260424/`.
2. Remove generated build output that can be recreated.
3. Verify the site still builds and TypeScript checks still pass.

## Safety Rules

- Keep `v2/`.
- Do not delete `v3/` because it has uncommitted changes.
- Archive source-like clutter instead of deleting it.
- Keep `.codex/`, `.planning/`, `.agents/`, source HTML pages, root site HTML pages, package/config files, fonts, and public assets.
