{
  "name": "sweed-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.7",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/web dev",
    "mastra:dev": "bun run --cwd apps/web mastra:dev",
    "mastra:build": "bun run --cwd apps/web mastra:build",
    "mastra:studio:deploy": "bun run --cwd apps/web mastra:studio:deploy",
    "mastra:server:deploy": "bun run --cwd apps/web mastra:server:deploy",
    "build": "bun run --cwd apps/web build",
    "start": "bun run --cwd apps/web start",
    "unit": "bun run --cwd apps/web unit",
    "typecheck": "bun run --cwd apps/web typecheck",
    "typecheck:packages": "tsc -p packages/tsconfig.json --noEmit",
    "lint": "bun run --cwd apps/web lint",
    "check": "bun run typecheck:packages && bun run typecheck && bun run lint && bun run unit",
    "smoke": "bun run --cwd apps/web smoke",
    "smoke:prod": "bun run --cwd apps/web smoke:prod",
    "capture:framer": "node scripts/capture-framer-site.mjs",
    "design:detect": "bunx impeccable detect apps/web/src/app apps/web/src/components apps/web/src/features/homepage apps/web/src/features/public-site apps/web/src/features/offer-funnel",
    "clean": "bun run --cwd apps/web clean"
  }
}
