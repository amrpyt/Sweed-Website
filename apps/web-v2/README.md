# SWEED Website V2

Standalone prototype for the new SWEED website direction.

Open directly:

```text
apps/web-v2/index.html
```

Or serve it locally:

```bash
bunx serve apps/web-v2
```

This folder is isolated from `apps/web` and does not change the current production website.
